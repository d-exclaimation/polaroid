import { component } from "@/lib/rc";
import { SafeAreaView, Text } from "react-native";

export default component(() => {
  return (
    <SafeAreaView>
      <Text>Settings</Text>
    </SafeAreaView>
  );
});
